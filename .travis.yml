language: generic

jobs:
  include:
      - stage: "Rocket Service"
        name: "Units tests of rocket service"
        install: 
          - pip install -r requirements.txt 
        script: 
          - cd services/rocket/test
          - python rocketApiTest.py
